{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://bamana.dev/spec/jsonschema/explode.schema.json",
  "title": "Bamana Explode Response",
  "description": "Contract for bamana explode JSON output.",
  "x-bamana-command": "explode",
  "x-bamana-contract-version": "0.1.0",
  "$defs": {
    "output": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "records_written", "record_start", "record_end"],
      "properties": {
        "path": { "type": "string" },
        "records_written": { "type": "integer", "minimum": 0 },
        "record_start": { "type": "integer", "minimum": 0 },
        "record_end": { "type": "integer", "minimum": 0 },
        "approx_compressed_start": { "type": ["integer", "null"], "minimum": 0 },
        "approx_compressed_end": { "type": ["integer", "null"], "minimum": 0 }
      }
    },
    "data": {
      "type": "object",
      "additionalProperties": false,
      "required": ["format", "input", "explode", "outputs", "index", "checksum_verification", "notes"],
      "properties": {
        "format": {
          "type": "string",
          "enum": ["BAM", "SAM", "FASTQ.GZ"]
        },
        "input": {
          "type": "object",
          "additionalProperties": false,
          "required": ["path"],
          "properties": {
            "path": { "type": "string" }
          }
        },
        "explode": {
          "type": "object",
          "additionalProperties": false,
          "required": ["requested_parts", "produced_parts", "strategy", "threads_used"],
          "properties": {
            "requested_parts": { "type": "integer", "minimum": 1 },
            "produced_parts": { "type": "integer", "minimum": 0 },
            "strategy": { "type": "string" },
            "threads_used": { "type": "integer", "minimum": 1 },
            "total_records": { "type": ["integer", "null"], "minimum": 0 },
            "checkpoints_available": { "type": ["integer", "null"], "minimum": 0 }
          }
        },
        "outputs": {
          "type": "array",
          "items": { "$ref": "#/$defs/output" }
        },
        "index": { "$ref": "./common/defs.schema.json#/$defs/indexInfo" },
        "checksum_verification": { "$ref": "./common/defs.schema.json#/$defs/checksumVerification" },
        "notes": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    }
  },
  "oneOf": [
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["ok", "command", "path", "data", "error"],
      "properties": {
        "ok": { "const": true },
        "command": { "const": "explode" },
        "path": { "$ref": "./common/defs.schema.json#/$defs/pathOrNull" },
        "analysis_wall_seconds": { "type": "number", "minimum": 0 },
        "data": { "$ref": "#/$defs/data" },
        "error": { "type": "null" }
      }
    },
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["ok", "command", "path", "data", "error"],
      "properties": {
        "ok": { "const": false },
        "command": { "const": "explode" },
        "path": { "$ref": "./common/defs.schema.json#/$defs/pathOrNull" },
        "analysis_wall_seconds": { "type": "number", "minimum": 0 },
        "data": { "oneOf": [{ "$ref": "#/$defs/data" }, { "type": "object" }, { "type": "null" }] },
        "error": { "$ref": "./common/json_error.schema.json" }
      }
    }
  ]
}

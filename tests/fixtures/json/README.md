# JSON Fixtures

Place generated JSON command outputs here when fixture-backed contract tests are
added.

These files should represent:

* real Bamana command output captured from controlled fixtures
* command/version combinations that matter for compatibility
* deliberate golden outputs that only change through contract review

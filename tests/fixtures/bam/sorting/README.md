# Sorting Fixtures

This directory is reserved for future fixtures that isolate sorting edge cases
without overloading the baseline valid BAMs.

Initial work should prefer the valid and transform fixture families before
adding sort-only variants here.

# Tag Fixtures

This directory is reserved for compact BAMs with deliberate auxiliary-tag
inventories.

Planned fixtures:

* `tiny.tags.nm_rg.bam`
* `tiny.tags.mixed_aux_types.bam`

These fixtures should stay small while still covering the aux traversal paths
needed by `check_tag`, `validate`, and tag-aware checksum modes.

# Valid BAM Fixtures

Planned high-value baseline fixtures:

* `tiny.valid.coordinate.bam`
* `tiny.valid.queryname.bam`
* `tiny.valid.unmapped.bam`

These fixtures are expected to remain tiny enough for fast CI execution and to
serve as the base material for several derived invalid or transform fixtures.

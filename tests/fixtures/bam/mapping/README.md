# Mapping Fixtures

This directory is reserved for future mapping-specialized fixtures, such as:

* mapped-heavy coordinate BAMs
* unmapped-only BAMs with distinct header/reference shapes
* deliberately contradictory mapping-state BAMs

The first tiny suite covers the core mapping semantics through the valid,
unmapped, and invalid baseline fixtures.

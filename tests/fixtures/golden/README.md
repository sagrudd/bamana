# Golden Output Fixtures

Place golden CLI help snapshots and future normalized output snapshots here when
the contract harness expands beyond the current inline checks.

Suggested future uses:

* top-level `--help` snapshots
* per-command help snapshots
* output captured from tiny BAM fixtures
* cross-version compatibility baselines

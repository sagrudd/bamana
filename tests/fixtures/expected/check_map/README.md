# Expected Outputs For `check_map`

Store mapping-assessment outputs here, including index-backed, scan-backed, and
indeterminate cases.

M12.5 reserves fallback outputs that must preserve
`index.diagnostic_status`, including:

* `tiny.invalid.mismatched_reference_count.csi.fallback.success.json`

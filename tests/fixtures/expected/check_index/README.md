# Expected Outputs For `check_index`

Store index inspection outputs here for valid, stale, missing, malformed,
unsupported, and mismatched-reference index states.

M12.5 reserves these additional stems:

* `tiny.invalid.mismatched_reference_count.csi.failure.json`

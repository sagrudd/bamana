# Expected Outputs For `summary`

Store bounded-scan and full-scan summary outputs here. Keep bounded results
explicitly sample-derived.

M12.5 reserves index-fallback outputs for CSI mismatch scenarios, including:

* `tiny.invalid.mismatched_reference_count.csi.fallback.success.json`

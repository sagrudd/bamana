# Expected Outputs For `explode`

Store planned explode outputs here, including shard manifests and checksum
verification expectations when the runtime command lands.

M12.5 reserves malformed FASTQ.GZI planner-sidecar behavior:

* `tiny.invalid.fastq_gz.bad_gzi.failure.json`

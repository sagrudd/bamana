process.executor = 'local'
docker.enabled = false

params.storage_context = 'shared_filesystem'
params.staging_mode = 'symlink'
params.include_staging_in_timing = false
params.reuse_materialized_inputs = true
